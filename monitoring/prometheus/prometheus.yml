global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'task-scheduler'
    static_configs:
      - targets: ['task-scheduler-service:8000']
    metrics_path: '/metrics'

rule_files:
  - "alerts.yml"
